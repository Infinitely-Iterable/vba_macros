# Macro List
1. toSqlValue - Content aware conversion of tabular data into sql value lines. I.e. If you have multiple rows to import and have the data in excel, this will add in the parentheses,
   commas, and single quotes for strings. It will compile all results into a single comma separated string that can then be pasted into your terminal or DB viewer of choice.
